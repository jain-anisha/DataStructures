#include<stdio.h>
#include<stdlib.h>

void swap(int* x, int* y)
{
    int temp = *x;
    *x = *y;
    *y = temp;
}

int main(int i, int array[x])
{
    /* code */
    // 
    printf("Enter number of elements in your list:");
    scanf("%d", &n);
    printf("Enter %d Numbersn", n);
    for (i = 0; i < n - 1; i++) {
        for(i = 0; i < n - 1; i++){
            pos = i;

        }
}
    return 0;
